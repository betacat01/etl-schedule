steps:
  - type: export
    ops:
      - mysql2hive:
          mysql_db:
          mysql_instance:
          hive_db:
          include_columns:
          exclude_columns:
          partition: p_day=${yesterday}
